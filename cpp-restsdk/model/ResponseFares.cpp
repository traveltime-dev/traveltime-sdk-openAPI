/**
 * TravelTime Platform API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * OpenAPI spec version: 1.0.0
 * Contact: support@igeolise.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 3.3.4.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "ResponseFares.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {

ResponseFares::ResponseFares()
{
}

ResponseFares::~ResponseFares()
{
}

void ResponseFares::validate()
{
    // TODO: implement validation
}

web::json::value ResponseFares::toJson() const
{
    web::json::value val = web::json::value::object();

    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Breakdown )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        val[utility::conversions::to_string_t("breakdown")] = web::json::value::array(jsonArray);
    }
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Tickets_total )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        val[utility::conversions::to_string_t("tickets_total")] = web::json::value::array(jsonArray);
    }

    return val;
}

void ResponseFares::fromJson(const web::json::value& val)
{
    {
        m_Breakdown.clear();
        std::vector<web::json::value> jsonArray;
        for( auto& item : val.at(utility::conversions::to_string_t("breakdown")).as_array() )
        {
            if(item.is_null())
            {
                m_Breakdown.push_back( std::shared_ptr<ResponseFaresBreakdownItem>(nullptr) );
            }
            else
            {
                std::shared_ptr<ResponseFaresBreakdownItem> newItem(new ResponseFaresBreakdownItem());
                newItem->fromJson(item);
                m_Breakdown.push_back( newItem );
            }
        }
    }
    {
        m_Tickets_total.clear();
        std::vector<web::json::value> jsonArray;
        for( auto& item : val.at(utility::conversions::to_string_t("tickets_total")).as_array() )
        {
            if(item.is_null())
            {
                m_Tickets_total.push_back( std::shared_ptr<ResponseFareTicket>(nullptr) );
            }
            else
            {
                std::shared_ptr<ResponseFareTicket> newItem(new ResponseFareTicket());
                newItem->fromJson(item);
                m_Tickets_total.push_back( newItem );
            }
        }
    }
}

void ResponseFares::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t("."))
    {
        namePrefix += utility::conversions::to_string_t(".");
    }

    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Breakdown )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        multipart->add(ModelBase::toHttpContent(namePrefix + utility::conversions::to_string_t("breakdown"), web::json::value::array(jsonArray), utility::conversions::to_string_t("application/json")));
            }
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Tickets_total )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        multipart->add(ModelBase::toHttpContent(namePrefix + utility::conversions::to_string_t("tickets_total"), web::json::value::array(jsonArray), utility::conversions::to_string_t("application/json")));
            }
}

void ResponseFares::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t("."))
    {
        namePrefix += utility::conversions::to_string_t(".");
    }

    {
        m_Breakdown.clear();

        web::json::value jsonArray = web::json::value::parse(ModelBase::stringFromHttpContent(multipart->getContent(utility::conversions::to_string_t("breakdown"))));
        for( auto& item : jsonArray.as_array() )
        {
            if(item.is_null())
            {
                m_Breakdown.push_back( std::shared_ptr<ResponseFaresBreakdownItem>(nullptr) );
            }
            else
            {
                std::shared_ptr<ResponseFaresBreakdownItem> newItem(new ResponseFaresBreakdownItem());
                newItem->fromJson(item);
                m_Breakdown.push_back( newItem );
            }
        }
    }
    {
        m_Tickets_total.clear();

        web::json::value jsonArray = web::json::value::parse(ModelBase::stringFromHttpContent(multipart->getContent(utility::conversions::to_string_t("tickets_total"))));
        for( auto& item : jsonArray.as_array() )
        {
            if(item.is_null())
            {
                m_Tickets_total.push_back( std::shared_ptr<ResponseFareTicket>(nullptr) );
            }
            else
            {
                std::shared_ptr<ResponseFareTicket> newItem(new ResponseFareTicket());
                newItem->fromJson(item);
                m_Tickets_total.push_back( newItem );
            }
        }
    }
}

std::vector<std::shared_ptr<ResponseFaresBreakdownItem>>& ResponseFares::getBreakdown()
{
    return m_Breakdown;
}

void ResponseFares::setBreakdown(const std::vector<std::shared_ptr<ResponseFaresBreakdownItem>>& value)
{
    m_Breakdown = value;
    
}

std::vector<std::shared_ptr<ResponseFareTicket>>& ResponseFares::getTicketsTotal()
{
    return m_Tickets_total;
}

void ResponseFares::setTicketsTotal(const std::vector<std::shared_ptr<ResponseFareTicket>>& value)
{
    m_Tickets_total = value;
    
}

}
}
}
}

