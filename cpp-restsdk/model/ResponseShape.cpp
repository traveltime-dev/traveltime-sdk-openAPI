/**
 * TravelTime Platform API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * OpenAPI spec version: 1.0.0
 * Contact: support@igeolise.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 3.3.4.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "ResponseShape.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {

ResponseShape::ResponseShape()
{
}

ResponseShape::~ResponseShape()
{
}

void ResponseShape::validate()
{
    // TODO: implement validation
}

web::json::value ResponseShape::toJson() const
{
    web::json::value val = web::json::value::object();

    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Shell )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        val[utility::conversions::to_string_t("shell")] = web::json::value::array(jsonArray);
    }
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Holes )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        val[utility::conversions::to_string_t("holes")] = web::json::value::array(jsonArray);
    }

    return val;
}

void ResponseShape::fromJson(const web::json::value& val)
{
    {
        m_Shell.clear();
        std::vector<web::json::value> jsonArray;
        for( auto& item : val.at(utility::conversions::to_string_t("shell")).as_array() )
        {
            if(item.is_null())
            {
                m_Shell.push_back( std::shared_ptr<Coords>(nullptr) );
            }
            else
            {
                std::shared_ptr<Coords> newItem(new Coords());
                newItem->fromJson(item);
                m_Shell.push_back( newItem );
            }
        }
    }
    {
        m_Holes.clear();
        std::vector<web::json::value> jsonArray;
        for( auto& item : val.at(utility::conversions::to_string_t("holes")).as_array() )
        {
            if(item.is_null())
            {
                m_Holes.push_back( std::vector<std::shared_ptr<Coords>>(nullptr) );
            }
            else
            {
                std::vector<std::shared_ptr<Coords>> newItem(std::vector<std::shared_ptr<Coords>>());
                newItem->fromJson(item);
                m_Holes.push_back( newItem );
            }
        }
    }
}

void ResponseShape::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t("."))
    {
        namePrefix += utility::conversions::to_string_t(".");
    }

    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Shell )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        multipart->add(ModelBase::toHttpContent(namePrefix + utility::conversions::to_string_t("shell"), web::json::value::array(jsonArray), utility::conversions::to_string_t("application/json")));
            }
    {
        std::vector<web::json::value> jsonArray;
        for( auto& item : m_Holes )
        {
            jsonArray.push_back(ModelBase::toJson(item));
        }
        multipart->add(ModelBase::toHttpContent(namePrefix + utility::conversions::to_string_t("holes"), web::json::value::array(jsonArray), utility::conversions::to_string_t("application/json")));
            }
}

void ResponseShape::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    utility::string_t namePrefix = prefix;
    if(namePrefix.size() > 0 && namePrefix.substr(namePrefix.size() - 1) != utility::conversions::to_string_t("."))
    {
        namePrefix += utility::conversions::to_string_t(".");
    }

    {
        m_Shell.clear();

        web::json::value jsonArray = web::json::value::parse(ModelBase::stringFromHttpContent(multipart->getContent(utility::conversions::to_string_t("shell"))));
        for( auto& item : jsonArray.as_array() )
        {
            if(item.is_null())
            {
                m_Shell.push_back( std::shared_ptr<Coords>(nullptr) );
            }
            else
            {
                std::shared_ptr<Coords> newItem(new Coords());
                newItem->fromJson(item);
                m_Shell.push_back( newItem );
            }
        }
    }
    {
        m_Holes.clear();

        web::json::value jsonArray = web::json::value::parse(ModelBase::stringFromHttpContent(multipart->getContent(utility::conversions::to_string_t("holes"))));
        for( auto& item : jsonArray.as_array() )
        {
            if(item.is_null())
            {
                m_Holes.push_back( std::vector<std::shared_ptr<Coords>>(nullptr) );
            }
            else
            {
                std::vector<std::shared_ptr<Coords>> newItem(std::vector<std::shared_ptr<Coords>>());
                newItem->fromJson(item);
                m_Holes.push_back( newItem );
            }
        }
    }
}

std::vector<std::shared_ptr<Coords>>& ResponseShape::getShell()
{
    return m_Shell;
}

void ResponseShape::setShell(const std::vector<std::shared_ptr<Coords>>& value)
{
    m_Shell = value;
    
}

std::vector<std::vector<std::shared_ptr<Coords>>>& ResponseShape::getHoles()
{
    return m_Holes;
}

void ResponseShape::setHoles(const std::vector<std::vector<std::shared_ptr<Coords>>>& value)
{
    m_Holes = value;
    
}

}
}
}
}

