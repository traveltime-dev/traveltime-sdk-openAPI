namespace OpenAPI.Model

open System
open System.Collections.Generic
open Newtonsoft.Json

module RequestRoutesProperty = 

  //#region RequestRoutesProperty

  let TravelTimeEnum = "travel_time"
  let DistanceEnum = "distance"
  let FaresEnum = "fares"
  let RouteEnum = "route"
  type RequestRoutesProperty = string
  
  //#endregion
  