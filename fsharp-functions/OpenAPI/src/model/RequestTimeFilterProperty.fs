namespace OpenAPI.Model

open System
open System.Collections.Generic
open Newtonsoft.Json

module RequestTimeFilterProperty = 

  //#region RequestTimeFilterProperty

  let TravelTimeEnum = "travel_time"
  let DistanceEnum = "distance"
  let DistanceBreakdownEnum = "distance_breakdown"
  let FaresEnum = "fares"
  let RouteEnum = "route"
  type RequestTimeFilterProperty = string
  
  //#endregion
  