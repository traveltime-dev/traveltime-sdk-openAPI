# TravelTime Platform API
#
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 1.0.0
# Contact: support@igeolise.com
# Generated by: https://openapi-generator.tech


#' ResponseFares Class
#'
#' @field breakdown 
#' @field tickets_total 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ResponseFares <- R6::R6Class(
  'ResponseFares',
  public = list(
    `breakdown` = NULL,
    `tickets_total` = NULL,
    initialize = function(`breakdown`, `tickets_total`, ...){
      local.optional.var <- list(...)
      if (!missing(`breakdown`)) {
        stopifnot(is.vector(`breakdown`), length(`breakdown`) != 0)
        sapply(`breakdown`, function(x) stopifnot(R6::is.R6(x)))
        self$`breakdown` <- `breakdown`
      }
      if (!missing(`tickets_total`)) {
        stopifnot(is.vector(`tickets_total`), length(`tickets_total`) != 0)
        sapply(`tickets_total`, function(x) stopifnot(R6::is.R6(x)))
        self$`tickets_total` <- `tickets_total`
      }
    },
    toJSON = function() {
      ResponseFaresObject <- list()
      if (!is.null(self$`breakdown`)) {
        ResponseFaresObject[['breakdown']] <-
          sapply(self$`breakdown`, function(x) x$toJSON())
      }
      if (!is.null(self$`tickets_total`)) {
        ResponseFaresObject[['tickets_total']] <-
          sapply(self$`tickets_total`, function(x) x$toJSON())
      }

      ResponseFaresObject
    },
    fromJSON = function(ResponseFaresJson) {
      ResponseFaresObject <- jsonlite::fromJSON(ResponseFaresJson)
      if (!is.null(ResponseFaresObject$`breakdown`)) {
        self$`breakdown` <- sapply(ResponseFaresObject$`breakdown`, function(x) {
          breakdownObject <- ResponseFaresBreakdownItem$new()
          breakdownObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          breakdownObject
        })
      }
      if (!is.null(ResponseFaresObject$`tickets_total`)) {
        self$`tickets_total` <- sapply(ResponseFaresObject$`tickets_total`, function(x) {
          tickets_totalObject <- ResponseFareTicket$new()
          tickets_totalObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          tickets_totalObject
        })
      }
    },
    toJSONString = function() {
      sprintf(
        '{
           "breakdown":
             [%s],
           "tickets_total":
             [%s]
        }',
        paste(unlist(lapply(self$`breakdown`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE))), collapse=","),
        paste(unlist(lapply(self$`tickets_total`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE))), collapse=",")
      )
    },
    fromJSONString = function(ResponseFaresJson) {
      ResponseFaresObject <- jsonlite::fromJSON(ResponseFaresJson)
      data.frame <- ResponseFaresObject$`breakdown`
      self$`breakdown` <- vector("list", length = nrow(data.frame))
      for (row in 1:nrow(data.frame)) {
          breakdown.node <- ResponseFaresBreakdownItem$new()
          breakdown.node$fromJSON(jsonlite::toJSON(data.frame[row,,drop = TRUE], auto_unbox = TRUE))
          self$`breakdown`[[row]] <- breakdown.node
      }
      data.frame <- ResponseFaresObject$`tickets_total`
      self$`tickets_total` <- vector("list", length = nrow(data.frame))
      for (row in 1:nrow(data.frame)) {
          tickets_total.node <- ResponseFareTicket$new()
          tickets_total.node$fromJSON(jsonlite::toJSON(data.frame[row,,drop = TRUE], auto_unbox = TRUE))
          self$`tickets_total`[[row]] <- tickets_total.node
      }
      self
    }
  )
)
