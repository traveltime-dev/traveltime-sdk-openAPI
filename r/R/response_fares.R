# TravelTime API
#
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 1.2.1
# Contact: support@igeolise.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ResponseFares
#'
#' @description ResponseFares Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field breakdown  list( \link{ResponseFaresBreakdownItem} ) 
#'
#' @field tickets_total  list( \link{ResponseFareTicket} ) 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ResponseFares <- R6::R6Class(
  'ResponseFares',
  public = list(
    `breakdown` = NULL,
    `tickets_total` = NULL,
    initialize = function(
        `breakdown`, `tickets_total`, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`breakdown`)) {
        stopifnot(is.vector(`breakdown`), length(`breakdown`) != 0)
        sapply(`breakdown`, function(x) stopifnot(R6::is.R6(x)))
        self$`breakdown` <- `breakdown`
      }
      if (!missing(`tickets_total`)) {
        stopifnot(is.vector(`tickets_total`), length(`tickets_total`) != 0)
        sapply(`tickets_total`, function(x) stopifnot(R6::is.R6(x)))
        self$`tickets_total` <- `tickets_total`
      }
    },
    toJSON = function() {
      ResponseFaresObject <- list()
      if (!is.null(self$`breakdown`)) {
        ResponseFaresObject[['breakdown']] <-
          lapply(self$`breakdown`, function(x) x$toJSON())
      }
      if (!is.null(self$`tickets_total`)) {
        ResponseFaresObject[['tickets_total']] <-
          lapply(self$`tickets_total`, function(x) x$toJSON())
      }

      ResponseFaresObject
    },
    fromJSON = function(ResponseFaresJson) {
      ResponseFaresObject <- jsonlite::fromJSON(ResponseFaresJson)
      if (!is.null(ResponseFaresObject$`breakdown`)) {
        self$`breakdown` <- ApiClient$new()$deserializeObj(ResponseFaresObject$`breakdown`, "array[ResponseFaresBreakdownItem]", loadNamespace("openapi"))
      }
      if (!is.null(ResponseFaresObject$`tickets_total`)) {
        self$`tickets_total` <- ApiClient$new()$deserializeObj(ResponseFaresObject$`tickets_total`, "array[ResponseFareTicket]", loadNamespace("openapi"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`breakdown`)) {
        sprintf(
        '"breakdown":
        [%s]
',
        paste(sapply(self$`breakdown`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`tickets_total`)) {
        sprintf(
        '"tickets_total":
        [%s]
',
        paste(sapply(self$`tickets_total`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(ResponseFaresJson) {
      ResponseFaresObject <- jsonlite::fromJSON(ResponseFaresJson)
      self$`breakdown` <- ApiClient$new()$deserializeObj(ResponseFaresObject$`breakdown`, "array[ResponseFaresBreakdownItem]", loadNamespace("openapi"))
      self$`tickets_total` <- ApiClient$new()$deserializeObj(ResponseFaresObject$`tickets_total`, "array[ResponseFareTicket]", loadNamespace("openapi"))
      self
    }
  )
)

