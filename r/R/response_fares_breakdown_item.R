# TravelTime Platform API
#
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 1.0.0
# Contact: support@igeolise.com
# Generated by: https://openapi-generator.tech


#' ResponseFaresBreakdownItem Class
#'
#' @field modes 
#' @field route_part_ids 
#' @field tickets 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ResponseFaresBreakdownItem <- R6::R6Class(
  'ResponseFaresBreakdownItem',
  public = list(
    `modes` = NULL,
    `route_part_ids` = NULL,
    `tickets` = NULL,
    initialize = function(`modes`, `route_part_ids`, `tickets`, ...){
      local.optional.var <- list(...)
      if (!missing(`modes`)) {
        stopifnot(is.vector(`modes`), length(`modes`) != 0)
        sapply(`modes`, function(x) stopifnot(R6::is.R6(x)))
        self$`modes` <- `modes`
      }
      if (!missing(`route_part_ids`)) {
        stopifnot(is.vector(`route_part_ids`), length(`route_part_ids`) != 0)
        sapply(`route_part_ids`, function(x) stopifnot(is.character(x)))
        self$`route_part_ids` <- `route_part_ids`
      }
      if (!missing(`tickets`)) {
        stopifnot(is.vector(`tickets`), length(`tickets`) != 0)
        sapply(`tickets`, function(x) stopifnot(R6::is.R6(x)))
        self$`tickets` <- `tickets`
      }
    },
    toJSON = function() {
      ResponseFaresBreakdownItemObject <- list()
      if (!is.null(self$`modes`)) {
        ResponseFaresBreakdownItemObject[['modes']] <-
          sapply(self$`modes`, function(x) x$toJSON())
      }
      if (!is.null(self$`route_part_ids`)) {
        ResponseFaresBreakdownItemObject[['route_part_ids']] <-
          self$`route_part_ids`
      }
      if (!is.null(self$`tickets`)) {
        ResponseFaresBreakdownItemObject[['tickets']] <-
          sapply(self$`tickets`, function(x) x$toJSON())
      }

      ResponseFaresBreakdownItemObject
    },
    fromJSON = function(ResponseFaresBreakdownItemJson) {
      ResponseFaresBreakdownItemObject <- jsonlite::fromJSON(ResponseFaresBreakdownItemJson)
      if (!is.null(ResponseFaresBreakdownItemObject$`modes`)) {
        self$`modes` <- sapply(ResponseFaresBreakdownItemObject$`modes`, function(x) {
          modesObject <- ResponseTransportationMode$new()
          modesObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          modesObject
        })
      }
      if (!is.null(ResponseFaresBreakdownItemObject$`route_part_ids`)) {
        self$`route_part_ids` <- ResponseFaresBreakdownItemObject$`route_part_ids`
      }
      if (!is.null(ResponseFaresBreakdownItemObject$`tickets`)) {
        self$`tickets` <- sapply(ResponseFaresBreakdownItemObject$`tickets`, function(x) {
          ticketsObject <- ResponseFareTicket$new()
          ticketsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          ticketsObject
        })
      }
    },
    toJSONString = function() {
      sprintf(
        '{
           "modes":
             [%s],
           "route_part_ids":
             [%s],
           "tickets":
             [%s]
        }',
        paste(unlist(lapply(self$`modes`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE))), collapse=","),
        paste(unlist(lapply(self$`route_part_ids`, function(x) paste0('"', x, '"'))), collapse=","),
        paste(unlist(lapply(self$`tickets`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE))), collapse=",")
      )
    },
    fromJSONString = function(ResponseFaresBreakdownItemJson) {
      ResponseFaresBreakdownItemObject <- jsonlite::fromJSON(ResponseFaresBreakdownItemJson)
      data.frame <- ResponseFaresBreakdownItemObject$`modes`
      self$`modes` <- vector("list", length = nrow(data.frame))
      for (row in 1:nrow(data.frame)) {
          modes.node <- ResponseTransportationMode$new()
          modes.node$fromJSON(jsonlite::toJSON(data.frame[row,,drop = TRUE], auto_unbox = TRUE))
          self$`modes`[[row]] <- modes.node
      }
      self$`route_part_ids` <- lapply(ResponseFaresBreakdownItemObject$`route_part_ids`, function (x) x)
      data.frame <- ResponseFaresBreakdownItemObject$`tickets`
      self$`tickets` <- vector("list", length = nrow(data.frame))
      for (row in 1:nrow(data.frame)) {
          tickets.node <- ResponseFareTicket$new()
          tickets.node$fromJSON(jsonlite::toJSON(data.frame[row,,drop = TRUE], auto_unbox = TRUE))
          self$`tickets`[[row]] <- tickets.node
      }
      self
    }
  )
)
