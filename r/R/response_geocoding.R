# TravelTime Platform API
#
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 1.0.0
# Contact: support@igeolise.com
# Generated by: https://openapi-generator.tech


#' ResponseGeocoding Class
#'
#' @field type 
#' @field features 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ResponseGeocoding <- R6::R6Class(
  'ResponseGeocoding',
  public = list(
    `type` = NULL,
    `features` = NULL,
    initialize = function(`type`, `features`, ...){
      local.optional.var <- list(...)
      if (!missing(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
        self$`type` <- `type`
      }
      if (!missing(`features`)) {
        stopifnot(is.vector(`features`), length(`features`) != 0)
        sapply(`features`, function(x) stopifnot(R6::is.R6(x)))
        self$`features` <- `features`
      }
    },
    toJSON = function() {
      ResponseGeocodingObject <- list()
      if (!is.null(self$`type`)) {
        ResponseGeocodingObject[['type']] <-
          self$`type`
      }
      if (!is.null(self$`features`)) {
        ResponseGeocodingObject[['features']] <-
          sapply(self$`features`, function(x) x$toJSON())
      }

      ResponseGeocodingObject
    },
    fromJSON = function(ResponseGeocodingJson) {
      ResponseGeocodingObject <- jsonlite::fromJSON(ResponseGeocodingJson)
      if (!is.null(ResponseGeocodingObject$`type`)) {
        self$`type` <- ResponseGeocodingObject$`type`
      }
      if (!is.null(ResponseGeocodingObject$`features`)) {
        self$`features` <- sapply(ResponseGeocodingObject$`features`, function(x) {
          featuresObject <- ResponseGeocodingGeoJsonFeature$new()
          featuresObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          featuresObject
        })
      }
    },
    toJSONString = function() {
      sprintf(
        '{
           "type":
             "%s",
           "features":
             [%s]
        }',
        self$`type`,
        paste(unlist(lapply(self$`features`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE))), collapse=",")
      )
    },
    fromJSONString = function(ResponseGeocodingJson) {
      ResponseGeocodingObject <- jsonlite::fromJSON(ResponseGeocodingJson)
      self$`type` <- ResponseGeocodingObject$`type`
      data.frame <- ResponseGeocodingObject$`features`
      self$`features` <- vector("list", length = nrow(data.frame))
      for (row in 1:nrow(data.frame)) {
          features.node <- ResponseGeocodingGeoJsonFeature$new()
          features.node$fromJSON(jsonlite::toJSON(data.frame[row,,drop = TRUE], auto_unbox = TRUE))
          self$`features`[[row]] <- features.node
      }
      self
    }
  )
)
