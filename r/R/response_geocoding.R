# TravelTime API
#
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 1.2.2
# Contact: support@igeolise.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ResponseGeocoding
#'
#' @description ResponseGeocoding Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field type  character 
#'
#' @field features  list( \link{ResponseGeocodingGeoJsonFeature} ) 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ResponseGeocoding <- R6::R6Class(
  'ResponseGeocoding',
  public = list(
    `type` = NULL,
    `features` = NULL,
    initialize = function(
        `type`, `features`, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`type`)) {
        stopifnot(is.character(`type`), length(`type`) == 1)
        self$`type` <- `type`
      }
      if (!missing(`features`)) {
        stopifnot(is.vector(`features`), length(`features`) != 0)
        sapply(`features`, function(x) stopifnot(R6::is.R6(x)))
        self$`features` <- `features`
      }
    },
    toJSON = function() {
      ResponseGeocodingObject <- list()
      if (!is.null(self$`type`)) {
        ResponseGeocodingObject[['type']] <-
          self$`type`
      }
      if (!is.null(self$`features`)) {
        ResponseGeocodingObject[['features']] <-
          lapply(self$`features`, function(x) x$toJSON())
      }

      ResponseGeocodingObject
    },
    fromJSON = function(ResponseGeocodingJson) {
      ResponseGeocodingObject <- jsonlite::fromJSON(ResponseGeocodingJson)
      if (!is.null(ResponseGeocodingObject$`type`)) {
        self$`type` <- ResponseGeocodingObject$`type`
      }
      if (!is.null(ResponseGeocodingObject$`features`)) {
        self$`features` <- ApiClient$new()$deserializeObj(ResponseGeocodingObject$`features`, "array[ResponseGeocodingGeoJsonFeature]", loadNamespace("openapi"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`type`)) {
        sprintf(
        '"type":
          "%s"
                ',
        self$`type`
        )},
        if (!is.null(self$`features`)) {
        sprintf(
        '"features":
        [%s]
',
        paste(sapply(self$`features`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(ResponseGeocodingJson) {
      ResponseGeocodingObject <- jsonlite::fromJSON(ResponseGeocodingJson)
      self$`type` <- ResponseGeocodingObject$`type`
      self$`features` <- ApiClient$new()$deserializeObj(ResponseGeocodingObject$`features`, "array[ResponseGeocodingGeoJsonFeature]", loadNamespace("openapi"))
      self
    }
  )
)

