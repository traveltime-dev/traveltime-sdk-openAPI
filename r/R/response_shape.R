# TravelTime API
#
# No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#
# The version of the OpenAPI document: 1.2.3
# Contact: support@igeolise.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title ResponseShape
#'
#' @description ResponseShape Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field shell  list( \link{Coords} ) 
#'
#' @field holes  list( \link{array[Coords]} ) 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ResponseShape <- R6::R6Class(
  'ResponseShape',
  public = list(
    `shell` = NULL,
    `holes` = NULL,
    initialize = function(
        `shell`, `holes`, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`shell`)) {
        stopifnot(is.vector(`shell`), length(`shell`) != 0)
        sapply(`shell`, function(x) stopifnot(R6::is.R6(x)))
        self$`shell` <- `shell`
      }
      if (!missing(`holes`)) {
        stopifnot(is.vector(`holes`), length(`holes`) != 0)
        sapply(`holes`, function(x) stopifnot(R6::is.R6(x)))
        self$`holes` <- `holes`
      }
    },
    toJSON = function() {
      ResponseShapeObject <- list()
      if (!is.null(self$`shell`)) {
        ResponseShapeObject[['shell']] <-
          lapply(self$`shell`, function(x) x$toJSON())
      }
      if (!is.null(self$`holes`)) {
        ResponseShapeObject[['holes']] <-
          lapply(self$`holes`, function(x) x$toJSON())
      }

      ResponseShapeObject
    },
    fromJSON = function(ResponseShapeJson) {
      ResponseShapeObject <- jsonlite::fromJSON(ResponseShapeJson)
      if (!is.null(ResponseShapeObject$`shell`)) {
        self$`shell` <- ApiClient$new()$deserializeObj(ResponseShapeObject$`shell`, "array[Coords]", loadNamespace("openapi"))
      }
      if (!is.null(ResponseShapeObject$`holes`)) {
        self$`holes` <- ApiClient$new()$deserializeObj(ResponseShapeObject$`holes`, "array[array[Coords]]", loadNamespace("openapi"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`shell`)) {
        sprintf(
        '"shell":
        [%s]
',
        paste(sapply(self$`shell`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`holes`)) {
        sprintf(
        '"holes":
        [%s]
',
        paste(sapply(self$`holes`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(ResponseShapeJson) {
      ResponseShapeObject <- jsonlite::fromJSON(ResponseShapeJson)
      self$`shell` <- ApiClient$new()$deserializeObj(ResponseShapeObject$`shell`, "array[Coords]", loadNamespace("openapi"))
      self$`holes` <- ApiClient$new()$deserializeObj(ResponseShapeObject$`holes`, "array[array[Coords]]", loadNamespace("openapi"))
      self
    }
  )
)
